[thrawn]
# integrator agents may dispatch without an interactive prompt
auto_fix = true

[checks]
# worktrees have no node_modules — install merged deps before checking
commands = ["bash -c 'npm install --no-audit --no-fund >/dev/null && make check'"]
