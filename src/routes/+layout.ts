// SPA mode: the app renders entirely client-side inside the Capacitor
// WebView — there is no server (PLAN.md §4). adapter-static's index.html
// fallback serves every route.
export const ssr = false;
export const prerender = false;
